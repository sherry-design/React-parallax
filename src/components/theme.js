import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#008080",
    },
    secondary: {
      main: "#3699ac",
    },
    tertiary: {
      main: "#a19e7d",
    },
    beigeColor: {
      main: "#FCEFD0",
    },
    extra: {
      main: "#e9d8a5",
    },
    error: {
      main: "#cc0000",
    },
  },
});
export default theme;